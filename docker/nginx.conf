worker_processes auto;
pid /run/nginx.pid;
daemon off;

events {
    worker_connections 4096;
    multi_accept on;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    # Logging
    access_log /dev/stdout;
    error_log /dev/stderr;

    # Performance
    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;
    keepalive_timeout 65;
    types_hash_max_size 2048;
    server_names_hash_bucket_size 128;

    # Gzip
    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_types text/plain text/css text/xml application/json application/javascript application/xml+rss application/atom+xml image/svg+xml;

    # FastCGI buffer sizes (for large Inertia responses)
    fastcgi_buffer_size 128k;
    fastcgi_buffers 4 256k;
    fastcgi_busy_buffers_size 256k;

    server {
        listen 8080;
        listen [::]:8080;
        server_name _;

        root /var/www/html/public;
        index index.php;

        charset utf-8;
        client_max_body_size 35M;

        add_header X-Content-Type-Options "nosniff";

        location / {
            try_files $uri $uri/ /index.php?$query_string;
        }

        location = /favicon.ico { access_log off; log_not_found off; }
        # robots.txt is a Laravel route, not a static file.
        location = /robots.txt  { access_log off; log_not_found off; try_files $uri /index.php?$query_string; }
        location = /up { access_log off; log_not_found off; try_files $uri $uri/ /index.php?$query_string; }

        error_page 404 /index.php;

        # Security: never execute PHP from writable paths (web-shell hardening).
        # storage/ is symlinked into the web root and is world-writable.
        location ~* ^/(storage|uploads)/.*\.php$ {
            deny all;
            return 403;
        }

        location ~ \.php$ {
            fastcgi_pass 127.0.0.1:9000;
            fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
            include fastcgi_params;

            fastcgi_param PHP_VALUE "upload_max_filesize=30M \n post_max_size=35M";

            # Buffer sizes for FastCGI
            fastcgi_buffer_size 128k;
            fastcgi_buffers 4 256k;
            fastcgi_busy_buffers_size 256k;
        }

        location ~ /\.(?!well-known).* {
            deny all;
        }

        # Cache static assets
        location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
            expires 1y;
            add_header Cache-Control "public, immutable";
            access_log off;
        }
    }
}